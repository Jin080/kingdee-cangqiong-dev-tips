# 贡献审核与整合规则

这份规则只用于长期审核团队投稿，并把值得保留的内容整合进正式 skill。

它不属于 skill 本体，不参与日常开发问答读取。

目标只有三个：

1. 正式 skill 越来越好用
2. 覆盖面越来越广
3. 文档不被一次性案例和项目私有噪音污染

## 审核结论分级

### A 级：可直接整合

满足条件：

- 结论正确
- 运行位置明确
- 前置条件明确
- 关键代码足够短且可复用
- 与现有内容不重复，或能明显补强现有内容

处理方式：

- 合并投稿 PR
- 本周直接纳入正式 skill

### B 级：补充后整合

常见原因：

- 缺少插件类型或事件入口
- 缺少前置条件
- 代码片段太长，但核心结论有价值
- 只写了结果，没写验证情况

处理方式：

- 可以先保留投稿 PR
- 不直接纳入正式 skill
- 先给提交人中文补充建议，等补充后再整合

### C 级：只保留贡献，不进正式 skill

常见原因：

- 内容基本正确，但强依赖项目私有字段、单据、客户约束
- 与现有正式 skill 重复，只是多了一个项目现场案例
- 价值更适合保留在 `contributions/` 供维护人参考

处理方式：

- 可以保留投稿记录
- 不进入正式 skill 主体

### D 级：退回或关闭

常见原因：

- 结论明显不可靠
- API 用法错误
- 没有验证
- 与仓库主题无关

处理方式：

- 不整合进正式 skill
- 必要时要求提交人重写

## 优化整合标准

把投稿整合进正式 skill 时，必须做下面这些优化，不允许原样搬运。

### 1. 从“案例”提炼成“模式”

保留：

- 通用场景
- 通用解法
- 关键 API
- 易错点

删除或替换：

- 客户名
- 项目名
- 业务编号
- 具体单据实例名
- 一次性排查过程

### 2. 代码只保留最小可复用片段

保留：

- 能说明核心做法的 5 到 20 行片段

删除：

- 整页无关代码
- 只为当前项目服务的上下文代码

### 3. 明确运行位置

整合后的结论必须能回答：

- 代码放在什么插件里
- 放在什么事件里
- 是界面层还是服务端

如果投稿没写清这一点，不能直接进入正式 skill。

### 4. 明确前置条件

整合后的结论必须能回答：

- 依赖什么字段
- 依赖什么元数据配置
- 是否要求基础资料属性已引用
- 是否依赖特定表单结构

### 5. 同类问题合并，不重复新增

如果现有正文已经有：

- 同类 API
- 同类事件
- 同类模式

优先做法是：

- 在原小节补充差异点
- 更新原示例

不要新增一段换汤不换药的内容。

## 什么时候值得进正式 skill

优先整合这些投稿：

- 多个项目都可能复用的问题
- 苍穹平台常见事件和 API 的典型用法
- 重复踩坑的场景
- 现有 skill 明显缺失的空白主题

谨慎整合这些投稿：

- 强依赖单个项目业务规则
- 只是在某个字段值上做了一次性映射
- 结论无法脱离项目上下文

## 正式 skill 的污染判定

出现以下情况，说明内容会污染正式 skill：

- 只保留了项目案例，没有抽象出通用结论
- 一个问题重复解释了同一组 API
- 代码片段长到需要读完整业务逻辑才能理解
- 一篇投稿只适用于单个客户环境
- 新增内容没有更新索引，后续难以检索

## 投稿归档建议

投稿统一按日期放在：

```text
contributions/YYYY-MM/
```

建议文件名格式：

```text
YYYY-MM-DD-主题关键词.md
```

例如：

```text
contributions/2026-04/2026-04-27-f7-parent-child.md
contributions/2026-04/2026-04-27-save-plugin.md
```

不要按人名拆目录。提交人信息写在投稿正文里即可。

## 主题归档建议

维护人整理投稿时，先判断这条内容最终更接近哪一类主题：

- 页面取值、赋值、列表、页面参数、单据体：归入 `pages`
- 事件联动、点击事件、父子页面、F7、前后端交互：归入 `interaction`
- DynamicObject、保存、查询、删除、ORM、过滤条件：归入 `backend-data`
- 提交、审核、刷新、提示、插件传参：归入 `operations`

如果一条投稿同时涉及多个主题，按“主问题”归类，只在正式整合时按需补充第 2 个主题，不要一篇投稿重复拆进多个地方。

## 正文拆分阈值

为了让 skill 越来越大但仍然可查，不要无条件新增文件。

优先级：

1. 先并入现有主题文件的已有小节
2. 没有合适小节时，新增二级或三级标题
3. 只有当某个主题文件持续膨胀到难以检索时，才拆新文件

建议阈值：

- 单个主题文件超过约 `900` 行，且新增内容已形成稳定子主题
- 同一稳定子主题累计至少 `3` 次可复用投稿

拆分后要求：

- 必须先更新 `skill/kingdee-cangqiong-dev-tips/references/index.md`
- 索引里明确写清楚“什么问题读什么文件”

## 整合后的索引义务

每次正式整合一条贡献，都要同步检查：

1. 主题归类是否准确
2. 新增关键词是否需要补进 `references/index.md`
3. 现有小节标题是否还能支撑后续检索
4. 是否出现“一个问题要反复读多个文件”的迹象

如果第 4 条已经频繁出现，说明索引粒度不够，需要细化。

## 中文反馈模板

给提交人的反馈必须用中文，并且要能直接指导补充，不要只写“请完善”。

推荐结构：

```text
结论：
这次贡献可以保留 / 需要补充后再整合 / 暂不建议进入正式 skill。

优点：
1. ...
2. ...

需要补充的点：
1. ...
2. ...

建议你这样修改：
1. ...
2. ...

原因：
这样修改后，维护人才能把它整理成可复用的正式 skill 内容，而不是只适用于当前项目的案例。
```

## 常见不足与中文建议

### 1. 缺运行位置

建议话术：

```text
你这次的结论方向是对的，但还缺少运行位置说明。请补充这段代码是放在界面插件、操作插件还是服务端逻辑里，以及具体触发事件是什么。否则后续整理进正式 skill 时，别人只知道 API，不知道应该落在哪个位置。
```

### 2. 缺前置条件

建议话术：

```text
请补充这个方案成立的前置条件，例如字段标识、单据体结构、是否依赖基础资料属性引用、是否需要元数据配置。正式 skill 不能只告诉别人“这么写”，还要告诉别人“在什么条件下这么写才成立”。
```

### 3. 代码片段过长

建议话术：

```text
当前代码片段过长，里面混入了不少项目上下文。请只保留最关键、最可复用的 5 到 20 行代码，把能说明问题的 API 和核心逻辑留下，其余业务代码删掉。这样后续整合后可读性会更高。
```

### 4. 过于项目私有

建议话术：

```text
这次内容更像项目案例记录，目前还不适合直接进入正式 skill。请把客户名、项目字段、业务编号去掉，改写成通用描述，并补 1 到 3 条“可整理进正式 skill 的结论”。维护人需要的是模式，不是现场纪要。
```

### 5. 与现有内容重复

建议话术：

```text
这次贡献和现有 skill 里的同类内容比较接近，暂时不建议原样新增。请补充你这次和现有结论相比，新增了什么差异点、边界条件或更稳妥的写法。只要能补出增量价值，后续仍然可以整合。
```

### 6. 未说明验证情况

建议话术：

```text
请补充这次方案的验证情况，例如在哪个插件、什么事件、什么页面结构下已经验证通过。没有验证信息的结论，维护人无法判断它是否足够稳定，通常不会直接进入正式 skill。
```

### 7. 主题归类不准确

建议话术：

```text
你这次投稿的内容主题还不够准确。它更偏向“页面取值/赋值”而不是“交互事件”，或者更偏向“后端数据操作”而不是“页面逻辑”。请把关键词和适用范围调整一下，方便后续路由到正确的参考文档。
```
