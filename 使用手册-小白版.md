# 金蝶苍穹 Skill 团队使用手册

这份文档只讲怎么用，不讲复杂原理。照着步骤做就可以。

## 这个仓库是做什么的

这个仓库用来统一维护团队的 `kingdee-cangqiong-dev-tips` skill。

你可以把它理解成：

- `main`：正式版，大家平时更新使用这个版本
- `develop`：收集版，同事提交的新经验先放这里
- `feature/xxx`：个人临时分支，每个人提交经验时先建一个自己的分支

普通同事日常只需要做两件事：

1. 安装或更新 skill
2. 遇到有价值的开发经验时，按模板提交

## 一、普通使用者：第一次安装

第一次使用时，只需要做一次。

### 第 1 步：打开 PowerShell

在 Windows 开始菜单搜索：

```text
PowerShell
```

打开后执行下面命令。

### 第 2 步：下载仓库

```powershell
git clone https://github.com/Jin080/kingdee-cangqiong-dev-tips.git "$env:USERPROFILE\.codex-skill-hub"
```

如果提示目录已经存在，说明你以前下载过，可以跳过这一步。

### 第 3 步：安装 skill 到本机

```powershell
powershell -ExecutionPolicy Bypass -File "$env:USERPROFILE\.codex-skill-hub\scripts\install-skill.ps1"
```

看到类似下面的输出就说明安装成功：

```text
Installed skill to C:\Users\你的用户名\.codex\skills\kingdee-cangqiong-dev-tips
```

## 二、普通使用者：以后每周更新

以后维护人通知大家“skill 已更新”时，你只需要执行这一行：

```powershell
powershell -ExecutionPolicy Bypass -File "$env:USERPROFILE\.codex-skill-hub\scripts\update-skill.ps1"
```

这个命令会自动做两件事：

1. 从 GitHub 拉取最新正式版
2. 同步到你本机的 Codex skills 目录

注意：只执行 `git pull` 不够。`git pull` 只会更新仓库目录，不会自动更新 Codex 真正读取的 skill 目录。

## 三、贡献者：提交一次开发经验

如果你在项目里遇到了一个金蝶苍穹 / BOS 开发问题，并且总结出了可复用经验，就按下面步骤提交。

下面示例假设：

- 你的名字拼音是 `zhangsan`
- 这次经验主题是 `f7-parent-child`
- 日期是 `2026-04-25`

实际使用时，把这些内容换成你自己的。

## 第 1 步：进入仓库目录

```powershell
cd "$env:USERPROFILE\.codex-skill-hub"
```

## 第 2 步：切到 develop 并拉最新代码

```powershell
git checkout develop
git pull origin develop
```

如果这是你第一次切换 `develop`，可以执行：

```powershell
git fetch origin
git checkout develop
```

## 第 3 步：新建自己的分支

```powershell
git checkout -b feature/zhangsan-f7-parent-child
```

分支命名建议：

```text
feature/你的名字-简短主题
```

例如：

```text
feature/lisi-save-plugin
feature/wangwu-dynamicobject-query
feature/zhaoliu-f7-filter
```

## 第 4 步：复制贡献模板

先确认当前月份目录是否存在。比如现在是 2026 年 4 月：

```powershell
New-Item -ItemType Directory -Force .\contributions\2026-04
```

然后复制模板：

```powershell
Copy-Item .\templates\contribution-template.md .\contributions\2026-04\2026-04-25-zhangsan-f7-parent-child.md
```

文件命名建议：

```text
contributions/年份-月份/日期-你的名字-简短主题.md
```

## 第 5 步：填写贡献内容

用编辑器打开刚才复制出来的文件：

```text
contributions/2026-04/2026-04-25-zhangsan-f7-parent-child.md
```

按模板填写：

- 问题场景：你当时遇到了什么问题
- 前置条件：这个方案在什么情况下适用
- 解决方案：最后怎么解决的
- 关键代码 / API：只贴最关键的代码
- 注意事项 / 常见误区：别人容易踩什么坑
- 可整理进正式 Skill 的结论：用 1 到 3 条话总结给维护人

不要把整个项目代码都贴进去。只写能复用的部分。

## 第 6 步：提交到本地 Git

```powershell
git add .\contributions\2026-04\2026-04-25-zhangsan-f7-parent-child.md
git commit -m "docs: add F7 parent-child communication notes"
```

提交信息建议格式：

```text
docs: add 简短英文主题
```

## 第 7 步：推送到 GitHub

```powershell
git push -u origin feature/zhangsan-f7-parent-child
```

推送成功后，GitHub 通常会提示你创建 Pull Request。

## 第 8 步：在 GitHub 创建 PR

打开仓库：

```text
https://github.com/Jin080/kingdee-cangqiong-dev-tips
```

创建 Pull Request 时注意：

- base / 目标分支：`develop`
- compare / 来源分支：你的 `feature/xxx` 分支

不要把 PR 提到 `main`。

## 四、维护人：每周整理发布

维护人每周做一次就可以，不需要每来一条经验就发布。

## 第 1 步：查看 develop 上的新贡献

维护人主要看：

```text
contributions/
```

里面的新 `.md` 文件。

## 第 2 步：判断哪些内容值得进正式 skill

不是所有贡献都要原样放进正式 skill。

适合整理进去的内容：

- 多个项目都可能遇到的问题
- 金蝶苍穹 / BOS 常见 API 用法
- 插件事件、页面交互、单据体处理、DynamicObject 查询等通用经验
- 容易踩坑但有明确解法的问题

不适合直接放进去的内容：

- 只对某个具体项目有效的临时处理
- 带有大量项目私有字段、客户名称、业务编号的内容
- 没有验证过的猜测
- 重复内容

## 第 3 步：整理进正式 skill

正式 skill 在这里：

```text
skill/kingdee-cangqiong-dev-tips/
```

常见更新位置：

```text
skill/kingdee-cangqiong-dev-tips/SKILL.md
skill/kingdee-cangqiong-dev-tips/references/pages.md
skill/kingdee-cangqiong-dev-tips/references/interaction.md
skill/kingdee-cangqiong-dev-tips/references/backend-data.md
skill/kingdee-cangqiong-dev-tips/references/operations.md
```

整理时尽量写成“通用解决方法”，不要原样复制同事的完整过程。

## 第 4 步：合并到 main

整理完成后，通过 PR 把 `develop` 的整理结果合并到 `main`。

`main` 就是所有同事最终更新使用的正式版。

## 第 5 步：通知大家更新

合并到 `main` 后，在群里通知大家执行：

```powershell
powershell -ExecutionPolicy Bypass -File "$env:USERPROFILE\.codex-skill-hub\scripts\update-skill.ps1"
```

## 五、常见问题

## 问题 1：为什么不能直接改 main？

`main` 是正式版。大家本机更新时拿的就是 `main`。

如果所有人都直接改 `main`，未经整理的内容会直接进入正式 skill，时间长了会越来越乱。

## 问题 2：为什么要先提交到 develop？

`develop` 是收集和整理用的地方。

同事先把经验提交到 `develop`，维护人再统一整理成更清晰、更通用的正式内容。

## 问题 3：为什么还要建 feature 分支？

`feature` 分支是你这一次提交经验的临时工作区。

这样每个人的改动互不影响，也方便 GitHub 做 PR 审核。

## 问题 4：执行 update 后还是没有生效怎么办？

先检查这个文件是否存在：

```powershell
Test-Path "$env:USERPROFILE\.codex\skills\kingdee-cangqiong-dev-tips\SKILL.md"
```

如果返回：

```text
True
```

说明 skill 已经同步到默认目录。

如果你使用了自定义 Codex 目录，需要带上 `-SkillRoot`：

```powershell
powershell -ExecutionPolicy Bypass -File "$env:USERPROFILE\.codex-skill-hub\scripts\update-skill.ps1" -SkillRoot "D:\Codex\skills"
```

## 六、最常用命令汇总

第一次安装：

```powershell
git clone https://github.com/Jin080/kingdee-cangqiong-dev-tips.git "$env:USERPROFILE\.codex-skill-hub"
powershell -ExecutionPolicy Bypass -File "$env:USERPROFILE\.codex-skill-hub\scripts\install-skill.ps1"
```

每周更新：

```powershell
powershell -ExecutionPolicy Bypass -File "$env:USERPROFILE\.codex-skill-hub\scripts\update-skill.ps1"
```

提交经验：

```powershell
cd "$env:USERPROFILE\.codex-skill-hub"
git checkout develop
git pull origin develop
git checkout -b feature/你的名字-简短主题
New-Item -ItemType Directory -Force .\contributions\2026-04
Copy-Item .\templates\contribution-template.md .\contributions\2026-04\2026-04-25-你的名字-简短主题.md
git add .\contributions\2026-04\2026-04-25-你的名字-简短主题.md
git commit -m "docs: add short topic"
git push -u origin feature/你的名字-简短主题
```
