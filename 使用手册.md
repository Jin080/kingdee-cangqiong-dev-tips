# 多 Skill 团队使用手册

这份文档按现在的正式流程来写，只讲应该怎么做，不展开讲 Git 原理。

适用对象：

- 普通同事：只安装和更新正式版 skill
- 投稿同事：把这次新增经验提交到仓库指定目录
- 维护人：每周整理投稿，发布正式版

## 一、先理解三个目录

这个仓库里最重要的只有三个目录：

- `skill/`
- `contributions/`
- `templates/`

作用分别是：

- `skill/`：正式版 skill 目录，下面可以放多个正式 skill，本机真正使用的是这里整理后的内容
- `contributions/`：同事投稿区，放原始经验文档
- `templates/`：投稿模板
- `corpus/`：heavy skill 语料发布清单和维护说明

当前正式 skill 例如：

- `skill/kingdee-cangqiong-dev-tips/`
- `skill/ai-coding-discipline/`
- `skill/kingdee-cangqiong-heavy/`
- `skill/kingdee-xinghan-heavy/`
- `skill/kingdee-xingkong-heavy/`

结论：

- 普通同事本地只需要安装正式版 skill
- 不需要把 `contributions/` 和 `templates/` 安装到本机 Codex skills 目录
- 投稿也不要直接改 `skill/`，统一先提到 `contributions/`

## 二、普通同事第一次安装

普通同事不需要 clone 整个仓库到本地。

第一次安装时，打开 PowerShell，执行：

```powershell
powershell -ExecutionPolicy Bypass -Command "& {
  $script = Join-Path $env:TEMP 'install-kingdee-workstation.ps1'
  Invoke-WebRequest -Uri 'https://raw.githubusercontent.com/Jin080/kingdee-cangqiong-dev-tips/main/scripts/install-workstation-from-github.ps1' -OutFile $script
  & powershell -ExecutionPolicy Bypass -File $script
}"
```

执行成功后，正式版 skill 会安装到默认目录：

```text
C:\Users\你的用户名\.codex\skills\
```

脚本会把仓库 `skill/` 目录下的全部正式 skill 同步到这个目录中。

如果本次安装包含 heavy skill，对应语料会默认下载到：

```text
D:\KingdeeDocs
```

如果目标机器没有 `D:` 盘，则自动改用：

```text
C:\Users\你的用户名\KingdeeDocs
```

如果目录不存在，脚本会自动创建。

如果你只想安装其中几个 skill，也可以带 `-SkillNames`。例如只安装：

- `kingdee-cangqiong-heavy`
- `kingdee-xinghan-heavy`

命令示例：

```powershell
powershell -ExecutionPolicy Bypass -Command "& {
  $script = Join-Path $env:TEMP 'install-kingdee-workstation.ps1'
  Invoke-WebRequest -Uri 'https://raw.githubusercontent.com/Jin080/kingdee-cangqiong-dev-tips/main/scripts/install-workstation-from-github.ps1' -OutFile $script
  & powershell -ExecutionPolicy Bypass -File $script -SkillNames 'kingdee-cangqiong-heavy','kingdee-xinghan-heavy'
}"
```

注意：

- `kingdee-cangqiong-dev-tips` 和 `ai-coding-discipline` 安装后即可直接使用
- `kingdee-cangqiong-heavy`、`kingdee-xinghan-heavy`、`kingdee-xingkong-heavy` 这 3 个 skill 依赖本地帮助中心语料库
- 其中星瀚 / 星空若要支持插件开发场景下的标识识别，还需要元数据索引文件：
  - `星瀚元数据-index.jsonl`
  - `星空元数据-index.jsonl`
- 第一次安装脚本会自动下载上述 heavy 语料和索引，并回写 3 个 skill 的 `references/config.md`
- 后续执行更新命令时，脚本会保留你本机已经改过的 `references/config.md`

如果你们某个人的 Codex skills 目录不是默认位置，可以让维护人单独给他带 `-SkillRoot` 的命令。

## 三、普通同事以后怎么更新

以后维护人每周整理发布后，普通同事只要执行一条命令：

```powershell
powershell -ExecutionPolicy Bypass -Command "& {
  $script = Join-Path $env:TEMP 'update-kingdee-skill.ps1'
  Invoke-WebRequest -Uri 'https://raw.githubusercontent.com/Jin080/kingdee-cangqiong-dev-tips/main/scripts/update-skill-from-github.ps1' -OutFile $script
  & powershell -ExecutionPolicy Bypass -File $script
}"
```

这条命令只会更新仓库 `skill/` 目录下的全部正式 skill，不会把下面这些内容装进本机 Codex skills 目录：

- `contributions/`
- `templates/`
- `README.md`
- 其他仓库管理文件

如果你不想每次输入命令，也可以直接双击安装包目录里的 `更新skill.bat`。

注意：

- 推荐发给同事的是一个完整安装包目录
- 安装包目录至少包含：
  - `安装skill和语料.bat`
  - `更新skill.bat`
  - `scripts/`
- 不要只单独发一个 `.bat`

原因：

- `.bat` 直接调用同目录 `scripts\*.ps1` 时最稳定
- 如果只剩单个 `.bat`，就只能临时去 GitHub 下载脚本再执行，更容易被网络、PowerShell 环境或安全软件影响

也就是说，普通同事本地长期只保留真正使用的 skill 主体。

后续更新只会更新 skill，不会重复下载 heavy 语料。

如果你只想更新其中几个 skill，也可以带 `-SkillNames`。例如：

```powershell
powershell -ExecutionPolicy Bypass -Command "& {
  $script = Join-Path $env:TEMP 'update-kingdee-skill.ps1'
  Invoke-WebRequest -Uri 'https://raw.githubusercontent.com/Jin080/kingdee-cangqiong-dev-tips/main/scripts/update-skill-from-github.ps1' -OutFile $script
  & powershell -ExecutionPolicy Bypass -File $script -SkillNames 'kingdee-xinghan-heavy','kingdee-xingkong-heavy'
}"
```

如果你要使用官方知识库 skill，还要额外确认：

- 你的电脑上已经存在帮助中心全量库
- 目录位置已经写入各 skill 的 `references/config.md`

例如，你本机如果把三套帮助中心放在：

```text
E:\AI\kingdee-docs
```

那么应保证下面目录存在：

- `E:\AI\kingdee-docs\苍穹帮助中心全量库`
- `E:\AI\kingdee-docs\星瀚帮助中心全量库`
- `E:\AI\kingdee-docs\星空帮助中心全量库`
- `E:\AI\kingdee-docs\星瀚元数据-index.jsonl`
- `E:\AI\kingdee-docs\星空元数据-index.jsonl`

并把这 3 个 skill 的 `BASE_PATH` 统一改成：

```text
E:\AI\kingdee-docs
```

如果你不想手工改这 3 个 `config.md`，可以直接运行语料安装脚本，让同事自己选择语料放置目录，并自动回写：

```powershell
powershell -ExecutionPolicy Bypass -Command "& {
  $script = Join-Path $env:TEMP 'install-kingdee-corpus.ps1'
  Invoke-WebRequest -Uri 'https://raw.githubusercontent.com/Jin080/kingdee-cangqiong-dev-tips/main/scripts/install-corpus-from-github.ps1' -OutFile $script
  & powershell -ExecutionPolicy Bypass -File $script -SourceRoot 'D:\已解压的语料包根目录' -CorpusRoot 'E:\AI\kingdee-docs'
}"
```

如果只想双击完成第一次安装，推荐把一个完整安装包目录发给同事，至少包含：

- `安装skill和语料.bat`
- `更新skill.bat`
- `scripts/`

含义：

- `SourceRoot`：当前语料包所在目录，目录下必须直接有：
  - `苍穹帮助中心全量库`
  - `星瀚帮助中心全量库`
  - `星空帮助中心全量库`
  - `星瀚元数据-index.jsonl`
  - `星空元数据-index.jsonl`
- `CorpusRoot`：最终想安装到本机哪个目录

脚本会做两件事：

1. 把三套语料目录和两个元数据索引复制到 `CorpusRoot`
2. 自动把 3 个 heavy skill 的 `references/config.md` 写成这个目录

如果语料已经提前放好了，只需要回写路径，则可省略 `SourceRoot`：

```powershell
powershell -ExecutionPolicy Bypass -Command "& {
  $script = Join-Path $env:TEMP 'install-kingdee-corpus.ps1'
  Invoke-WebRequest -Uri 'https://raw.githubusercontent.com/Jin080/kingdee-cangqiong-dev-tips/main/scripts/install-corpus-from-github.ps1' -OutFile $script
  & powershell -ExecutionPolicy Bypass -File $script -CorpusRoot 'E:\AI\kingdee-docs'
}"
```

## 四、普通同事怎么投稿

普通同事如果有新的开发经验要提交，不要求本地 clone 仓库，直接在 GitHub 网页操作即可。

前提：

- 你已经被加入这个仓库，至少有 `Write` 权限

如果你没有被加入仓库，就不能直接在这个仓库里创建分支和 PR。

## 五、投稿前先准备什么

每次投稿前先准备三样东西：

1. 你的姓名拼音
2. 本次问题的简短主题
3. 一份按模板写好的经验内容

例如：

- 姓名拼音：`zhangsan`
- 简短主题：`f7-parent-child`
- 日期：`2026-04-27`

## 六、投稿文件应该放哪里

投稿文件统一放到：

```text
contributions/YYYY-MM-DD/
```

例如：

```text
contributions/2026-04-30/f7-parent-child.md
contributions/2026-04-30/save-bill-entry.md
```

如果你这次有多个优化内容，可以一次提交多个 `.md` 文件。

例如同一个人这次有 3 篇经验，可以一起放在同一天的投稿目录下，然后一次提交成一个 PR。

文件名不强制统一格式，只要能看出主题即可。

不要放错位置：

- 不要放到 `templates/`
- 不要放到 `skill/`
- 不要放到仓库根目录

## 七、GitHub 网页投稿步骤

### 第 1 步：打开模板

进入：

```text
templates/contribution-template.md
```

把模板内容复制出来。

### 第 2 步：进入投稿目录

进入本月投稿目录，例如：

```text
contributions/2026-04-30/
```

如果目录还不存在，就在 GitHub 网页里新建。

### 第 3 步：新建投稿文件

新建文件，文件名可以自由命名，但建议能看出主题，例如：

```text
f7-parent-child.md
save-plugin.md
```

例如：

```text
2026-04-27-zhangsan-f7-parent-child.md
```

### 第 4 步：粘贴模板并填写

把模板内容粘进去，再按真实情况填写：

- 问题场景
- 前置条件
- 解决方案
- 关键代码 / API
- 注意事项 / 常见误区
- 可整理进正式 Skill 的结论

要求：

- 写清楚问题和结论
- 不要贴整个项目代码
- 尽量去掉客户名、业务编号、敏感项目字段

### 第 5 步：提交时不要直接改主分支

在 GitHub 网页保存时，选择：

```text
Create a new branch for this commit and start a pull request
```

不要选：

```text
Commit directly to the main branch
```

### 第 6 步：创建 PR

提交后 GitHub 会自动引导你创建 PR。

目标分支必须是：

```text
develop
```

不要直接提到：

```text
main
```

## 八、如果一次有多个文件，能不能一起提

可以。

如果你这次总结了多个问题，可以在同一个投稿目录下连续新建多个 `.md` 文件，然后一次发一个 PR。

例如：

```text
contributions/2026-04-30/f7-parent-child.md
contributions/2026-04-30/dynamicobject-query.md
contributions/2026-04-30/save-plugin.md
```

维护人会在一个 PR 里统一看到这些增量内容。

但有个要求：

- 同一个 PR 最好只提交同一批次、同一主题范围内的经验

不要把完全无关的历史修改混在一个 PR 里。

## 九、维护人每周怎么整理

维护人每周只做一次集中整理，不需要每来一篇投稿就发布。

标准流程：

1. 查看 `develop` 中本周新增的投稿文档
2. 判断哪些内容值得进入正式版 skill
3. 去重、合并同类项、删掉项目私有噪音
4. 把可复用内容整理进对应的正式 skill 目录，并同步更新该 skill 的索引或路由说明
5. 发起 PR 合并到 `main`
6. 发布后通知同事执行更新命令

## 十、为什么普通同事不要直接改正式版 skill

因为投稿文档和正式版 skill 的目标不同。

投稿文档强调：

- 记录这次问题是怎么解决的

正式版 skill 强调：

- 下次别人怎么快速复用这个结论

所以流程必须是：

```text
原始经验 -> contributions -> 维护人整理 -> skill 正式版 -> main 发布
```

而不是：

```text
想到什么就直接往正式 skill 里堆
```

后者一定会越来越乱。

## 十一、谁需要仓库权限

因为你们决定让团队成员直接在这个仓库投稿，所以：

- 团队同事需要加入仓库作为协作者
- 权限建议给 `Write`

`Write` 权限已经足够让他们：

- 在 GitHub 网页创建分支
- 新建投稿文件
- 发起 PR

不需要给普通同事：

- `Admin`

## 十二、规则怎么配合

建议保持下面这套规则：

- `main` 禁止直接 push
- `develop` 禁止直接 push
- 普通同事必须通过 PR 投稿到 `develop`
- 维护人账号加入 ruleset 的 `Bypass list`

这样效果最符合你的场景：

- 普通同事不能乱改正式分支
- 维护人每周整理发布时不需要再找别人审批

## 十三、最常用的两条命令

### 第一次安装

```powershell
powershell -ExecutionPolicy Bypass -Command "& {
  $script = Join-Path $env:TEMP 'install-kingdee-skill.ps1'
  Invoke-WebRequest -Uri 'https://raw.githubusercontent.com/Jin080/kingdee-cangqiong-dev-tips/main/scripts/install-skill-from-github.ps1' -OutFile $script
  & powershell -ExecutionPolicy Bypass -File $script
}"
```

### 后续更新

```powershell
powershell -ExecutionPolicy Bypass -Command "& {
  $script = Join-Path $env:TEMP 'update-kingdee-skill.ps1'
  Invoke-WebRequest -Uri 'https://raw.githubusercontent.com/Jin080/kingdee-cangqiong-dev-tips/main/scripts/update-skill-from-github.ps1' -OutFile $script
  & powershell -ExecutionPolicy Bypass -File $script
}"
```

这两条命令就是普通同事最常用的全部本地操作。
